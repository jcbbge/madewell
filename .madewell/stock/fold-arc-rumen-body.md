# Fold: lab quality stop → Ground + Jig

## Making
Instruments in kernel. Hooks wired by install. Correction log restored (proposed − accepted).
Metabolism harness absorbed. `mw-jigs.sh` runs registry detectors.

## Not making
Harness slash skills. Pretending git log is the differential. Optional gate (SPEC already said JIG).

## Done when
`mw-hooks.sh` installs pre-commit + post-commit. `mw-record.sh` consumes `proposed.json`.
`sh .madewell/bin/mw-tax.sh` reads live jsonl. `node .madewell/jig/metabolism/live-feed.mjs` passes.

## Waits on
—
