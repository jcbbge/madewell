# Decisions

One line per decision that would be expensive to rediscover. Newest first.

| Date | Decision | Why |
|---|---|---|
| 2026-08-27 | Fold the peerlab C board into Made Well as `bin/mw-move`, not as a second work OS. Three states (stock/bench/finished). Four legal moves. Stages, never commits. Spawn stays outside this repo. | `open/held/done/verified` was a fourth kind of matter and a rewind. The C value (atomic claim, ENOENT fence, no daemon) lives as `git mv` that refuses the rest. Delete the binary: files remain. |
| 2026-08-27 | Tax = `mw-tax.sh` on live `corrections.jsonl` + `firings.jsonl`. Override contact is `proposed.json` (consumed at commit). Fixture 6/6 is function-rot only. RATIFY never auto-builds. | Absorb is Σ(tax) > fence cost on real proposed−accepted, not a synthetic table. |
| 2026-08-27 | Ground is `.madewell/ground/`. The Jig is `.madewell/jig/` plus hooks: `mw-gate` / `mw-jigs` pre-commit, `mw-record` post-commit. `corrections.jsonl` is proposed−accepted (not piece position). Metabolism harness absorbed from the lab. | Arc proved the organ. Install must wire it. Git log is not the differential. |
