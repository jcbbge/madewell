# Decisions

One line per decision. Append only. Never delete.

Format: `YYYY-MM-DD | decision | reason`

---

2026-05-21 | use Made Well for this project | structured workflow, no cold starts, state persists across sessions
2026-05-21 | start with the smallest useful version | prove it works for one person before adding scope
2026-05-30 | validation is the fourth atomic phase of all work, executed by independent sub-agents | every initiative runs Ideate → Plan → Implement → Validate. The builder never validates their own work — structural separation between builder and verifier is the only defense against self-marking success. Validation criteria are pluggable "jump packs": coding has one, sales/marketing/ops would each have their own. The four-role shape (Builder, Designer, Runner, Triage) is universal; the verification content swaps per domain.
2026-05-30 | coding jump pack — independent test-design, test-run, and failure-triage sub-agents | an agent that writes its own tests will fudge them to pass. Test-Designer runs in parallel with Implementer from the same brief. Test-Runner is read-only on code/tests. Failure-Triage is a fresh agent that classifies failures as (a) pre-existing bug, (b) wrong implementation, or (c) wrong test. Applies to code-generating work only; non-code briefs declare Testing: Applies: no with reason.
2026-06-14 | the rubric is "Does this lead to craft, beauty, and care?" — one root question, system-wide, including the agent's prime directive | unifies three drifting rubric statements into one constitution; craft/beauty/care is the standard, and the sum of those is something the person is proud of
2026-06-14 | Made Well is a two-audience system with two root entry doors | the human door (MADEWELL.md) orients the person — who/what/why, expectations, "it works if you go slow"; the agent door (the root instruction file) boots the agent. Keep them separate; never collapse the human entry into the agent system. System contents live in their own directory.
2026-06-14 | a first-run Orientation phase precedes Discovery | before asking someone what they want to build, the mentor sets the frame — what's about to happen, their role and the agent's, why deliberate beats fast. Runs ONCE on the first session then goes quiet (initialize-then-embody). Gives the person informed control.
2026-06-14 | the agent instruction file lives at the repo ROOT in the runtime's convention | runtimes auto-load a root file (CLAUDE.md for Claude Code; AGENTS.md as the cross-tool standard). On first contact the agent ensures the correct root file exists so Made Well loads next session, regardless of provider. Provider-agnostic by design.
2026-06-14 | reliability for the non-technical user outranks leanness in Made Well's own construction | the audience must never carry cognitive overhead to run the system — it runs smoothly, consistently, reliably on their behalf, invisibly. When elegance/leanness and reliable-for-the-user conflict, choose reliable. This is the project throughline (see AGENTS.md "The Throughline"); it is why every-session essentials stay in the auto-loaded AGENTS.md rather than thinned to on-demand skill pointers.
2026-06-20 | Made Well and Rumen relate as host↔organ, not as a stack | neither is the other's foundation: Made Well runs without Rumen, and Rumen runs naked in Arc with no Made Well — two things that each stand alone can't be foundation-and-superstructure. Made Well is the HOST (the work loop + persistence + the Guide persona); Rumen is the quality ORGAN it ships pre-installed and can run without. They meet at exactly two touchpoints — Verify (check work against the walls/assays) and Land (hand over proposed−accepted as cud). Rumen is the PRO-PACK implementation of Made Well's quality shape: the Enforcer skill is the batteries-included default behind the SAME contract. The dependency is one-way and optional — Made Well → (optional) → Rumen across the contract only; Made Well never imports Rumen, never writes to .rumen/, never breaks when it's absent. Resolves the contradiction where Rumen's PACK-CONTRACT §5 made the engine the foundation and Made Well's MERGE-INVENTORY made it a component — it is neither; it is a transplantable organ. This closes the former R15 engine↔kernel seam: the seam IS the quality contract, not a wrapping order.
2026-06-20 | the unit of work has a FOUR-leg lifecycle: Discovery → Commit → Cycle → Land, wrapping the four-phase execution core | the four phases (Imagine→Plan→Make→Verify) are the skeleton/core ("Cycle"), fractal and recurring at every altitude. Around them: Discovery (divergent intake — "what deserves attention"), Commit (convergent admission gate — "this, not that, now"; the WIP limiter), Land (the outlet — Ship the DELTA outward + Reflect the nutrient inward; carries single-loop feedback→Discovery and double-loop feedback→process). The four legs ALTERNATE anabolic↔catabolic (take-in / cut-down / build / release), which is why four is stable — opposite legs balance like a table; the old system was a two-legged table (all intake, no release) and fell over. Discovery and Land are boundary membranes (an adjoint pair — the formal reason consume/produce must balance); Commit is the newly-named admission gate (the catabolic-convergent face that was missing); the Cycle is the core. "Pillars" is freed and reserved for the dev-pack's four software DOMAINS (backend/frontend/api/ci-cd) — a different axis (what you build, not when).
2026-06-20 | catabolism is the missing primitive across the whole system; Rumen is its reference implementation | every layer has an anabolic intake but no catabolic release, and that single asymmetry is the root of every flooding/backup hit so far: work has Discovery but no Land (WIP floods), attention has capture but no Commit-gate (active-list thrash), conventions had promotion but no apoptosis (the rule pile — SOLVED in Rumen), memory has capture but no forgetting (context rot). One disease, many masks. Catabolism is NOT destruction — it is the controlled breakdown that returns nutrients and frees capacity (ship+drain, say-no/descope, forget, slough are the same operation in different substrates). Rumen's mechanic is the TEMPLATE every layer copies: earn your place by economics, die when you stop paying, sunset-review to distinguish "internalized" from "still-needed-but-silent." KERNEL LAW: every contract in the lattice must expose a catabolic method, modeled on Rumen.
2026-06-20 | Made Well is a contract lattice, not a feature set | the kernel = a small set of irreducible contracts (Intake/Discovery, Commit, Cycle, Outlet/Land, Memory, Quality, Persona) + the cycle topology that wires them + the dumbest-possible default implementation of each. Everything else — the Guide persona, the dev pack, Rumen, a memory backend, the thinking-tools — is a swappable implementation behind a contract. The kernel depends ONLY on contracts, never on a specific implementation; plug-and-play (swap my opinionated default for someone else's) falls out for free. Every shape therefore has the same four-slot structure: contract · batteries-included default · my pro pack · open-ecosystem slot. Persona-as-module, host↔organ (Rumen), and the memory-substrate question are all the same move applied to different rows.
2026-06-20 | the Guide persona is a pack, not the kernel | extracted from AGENTS.md into .madewell/packs/guide/PACK.md (the Throughline, Who-You-Are/persona, Reading the Territory, the Four Altitudes, Teaching Through Doing, Orientation, the question-first session-start). The kernel now runs PERSONA-FREE by default — a competent, terse operator that drives the work lifecycle and says only what's useful (the naked install, how Arc would consume it). The function (Orchestrator) stays in the kernel and is identical in every install; only how the system *sounds and teaches* swaps. This is the first row of the contract lattice extracted in practice: Persona contract · default = plain operator · pro = the Guide. The Guide stays the recommended load for non-technical builders (the product's designed face); auto-loading it for the human profile is follow-on wiring. Proves the lattice: persona detaches without touching the operational core.
2026-06-20 | the Persona row is a SLOT with named registers, filled per profile — not a binary default/pro | refines the entry above. Three non-peer fills: NAKED (the persona-free kernel — a substrate for machines/CI/agents; no human meets it raw), GUIDE (novice-human register; Made Well's batteries-included default; `packs/guide/`), CONTRIBUTOR (expert-human register — terse peer engineer, law-not-suggestion, exemplar-bound; lifted from Arc's AGENTS.md voice into `packs/dev/persona.md`). Persona is a DEPENDENT variable of audience, and audience is declared by the profile/primary-pack: the Contributor register TRAVELS WITH THE DEV PACK because whoever loads the software domain is by default technical. Orthogonal in the contract (you CAN mix Guide register + dev domain for a non-technical founder building software); bundled-by-default in the profile. The PROFILE is the bundling layer (already ratified: "a profile = which cartridges are loaded") — it binds persona + domain pack + quality + memory at once: Made-Well-default = {Guide + batteries-included}; Arc's "contributor module" = {Contributor + dev pack + Rumen} = "the upgraded Made Well." General rule: a domain pack MAY carry the register its audience needs (sales→operator voice, marketing→brand voice). This validates the persona extraction as the ENABLING move — only a persona-free kernel lets Arc speak a different voice than the founder.
2026-06-20 | the technical register has TWO insertion points — Lead and Contributor — set by a second axis (project-relationship); corrects the single "Contributor register" from earlier today | persona depends on TWO independent axes: software expertise (novice↔expert) AND project relationship (owner↔guest — do you hold the project's reality in your head, or must you be onboarded into it?). Guide = novice+owner (a founder building their own thing). The dev pack serves the two EXPERT points: LEAD (expert+owner — the solo maintainer; works as a peer with the agent, "me and my agent stepping through together"; no onboarding; makes the architecture/convention calls) and CONTRIBUTOR (expert+guest — a contractor/new hire + their agent folded into an EXISTING project; onboarded on first contact; defers the Lead's calls, surfaces findings, stays in its lane). CORRECTION: the terse peer voice I labeled "Contributor" earlier is really the shared OPERATING register; the distinguishing Contributor traits are being onboarded + deferring authority, not the voice. Both live in packs/dev/persona.md.
2026-06-20 | onboarding is an emergent read-out of the system's accumulated memory, NOT a doc the Lead writes | the acute pain (solo lead): onboarding cost is paid AGAIN for every hire — hand-written setup docs, teaching the guest's agent the system, teaching the human to use their agent — out of band, in time/tokens/money. The fix: the system already holds everything a newcomer needs because the Lead produced it by working — scope=PRODUCT.md, roadmap=STATE+backlog+git handoffs, conventions=DECISIONS+Rumen packs/lexicon, workflow=the four-leg lifecycle, env=project setup. Contributor onboarding (packs/dev/onboarding.md, first-contact-only) ASSEMBLES the walk-through live from those — always current, ~zero marginal Lead-cost. The guest's agent doesn't get taught the project, it LOADS it (already plugged in via AGENTS.md + .madewell/ + the packs). Lead pays encoding ONCE; every guest is onboarded from it automatically. Same Rumen move (document layer → system layer) applied to onboarding.
2026-06-20 | the Arc handshake DOCKED — the Contributor profile + dev-pack coordination surface are validated by a real conformant instance | a 4-pass, human-relayed negotiation joined two independently-built systems: the top-down framework (contracts/lattice/profiles) and Arc's bottom-up .collab/ contributor cartridge. Outcome: the framework NAMES every seam (persona registers, onboarding read-out, escalation ladder, brief schema, status loop, the Commit gate) and Arc's .collab/ is its conformant instance — convergence tax ~0 (Arc's battle-tested escalation ladder + _TEMPLATE.md became the framework's canonical escalation.md + brief-template.md VERBATIM; the A.3 ruling resolved into the Commit gate via Arc's own `_`-prefix exclusion). KEY BOUNDARY clarified at close: the framework is a CONTRACT/methodology (vendored into a project with `canonical: madewell/… @ <commit>` provenance headers = transport option (a)), NOT a runtime — project data (status.jsonl, briefs, tax.jsonl) lives in the project; the framework owns only the SCHEMAS. The status loop is intra-project (Contributor emits → Lead consumes), so there is NO cross-repo data flow. The only deferred item is an OPTIONAL drift-gate (CI re-checking vendored copies against canonical), parked under the future "how a project installs Made Well" distribution thread. Lesson: when a real instance exists, generalize FROM it (host the instance's proven text as canonical), don't invent over it.
