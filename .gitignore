# macOS
.DS_Store
**/.DS_Store

# Editor / OS cruft
*.swp
*~
.idea/
.vscode/

# Node tooling (if added later)
node_modules/

# Made Well — the active profile is a local, per-clone choice (never committed)
.madewell/profile

# Scaffolding binary (source is bin/mw-move.c)
.madewell/bin/mw-move
.madewell/bin/mw-move.dSYM/

# Made Well — this repo's own instance ledgers (local, never committed).
# A consumer gets the same protection from kernel/jig/.gitignore, seeded by install.
.madewell/jig/corrections.jsonl
.madewell/jig/firings.jsonl
.madewell/jig/proposed.json
.madewell/profile
